apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  type: NodePort # Changed from LoadBalancer to NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000 # Optional: You can test specidsdfdfdsfy afdsdsdsd port in the range 30000-32767.
                        # If omitted, Kubernetes will automatically assign one.
